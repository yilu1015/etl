name: etl
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - -e .
    - pytest>=7.0.0
    - pytest-cov>=3.0.0
    - black>=22.0.0
    - flake8>=4.0.0
    - isort>=5.10.0
    - mypy>=0.950
    - jupyter>=1.0.0
    - jupyterlab>=3.0.0
